<odoo>

    <!-- Full custom login layout -->
    <template id="skillbridge_login_form" inherit_id="web.login" priority="1000">
        <xpath expr="//t[@t-call='web.login_layout']" position="replace">
            <t t-call="web.frontend_layout">
                <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
                <t t-set="body_classname" t-value="'bg-100'"/>
                <t t-set="no_header" t-value="True"/>
                <t t-set="no_footer" t-value="True"/>

                <div class="sb-shell">
                    <div class="sb-card">
                        <div class="sb-card-inner">
                            <div class="sb-brand-block">
                                <div class="sb-logo-wrap">
                                    <img t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}" alt="SkillBridge logo"/>
                                </div>
                                <h2>Eduskillbridge Studio</h2>
                            </div>
                            <div class="sb-form-wrapper">
                                <form class="oe_login_form sb-login-panel" role="form"
                                      t-attf-action="/web/login"
                                      method="post"
                                      onsubmit="this.action = '/web/login' + location.hash">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                    <div class="mb-3" t-if="databases and len(databases) &gt; 1">
                                        <label for="db" class="col-form-label">Database</label>
                                        <div class="input-group">
                                            <input type="text" name="db" t-att-value="request.db" id="db" class="form-control" required="required" readonly="readonly"/>
                                            <a role="button" href="/web/database/selector" class="btn btn-secondary">Select <i class="fa fa-database" role="img" aria-label="Database" title="Database"></i></a>
                                        </div>
                                    </div>

                                    <div class="mb-3 field-login">
                                        <label for="login" class="form-label">Email</label>
                                        <input type="text" placeholder="Email" name="login" t-att-value="login" id="login" class="form-control" required="required" autocomplete="username" autofocus="autofocus" autocapitalize="off"/>
                                    </div>

                                    <div class="mb-3">
                                        <label for="password" class="form-label">Password</label>
                                        <input type="password" placeholder="Password" name="password" id="password" class="form-control" required="required" autocomplete="current-password" t-att-autofocus="'autofocus' if login else None" maxlength="4096"/>
                                    </div>

                                    <p class="alert alert-danger" t-if="error" role="alert">
                                        <t t-esc="error"/>
                                    </p>
                                    <p class="alert alert-success" t-if="message" role="status">
                                        <t t-esc="message"/>
                                    </p>

                                    <div class="clearfix oe_login_buttons text-center gap-1 d-grid mb-1 pt-3">
                                        <button type="submit" class="btn sb-primary-btn w-100">Log in</button>
                                        <t t-if="debug">
                                            <button type="submit" name="redirect" value="/web/become" class="btn btn-link btn-sm">Log in as superuser</button>
                                        </t>
                                        <div class="o_login_auth sb-social-slot"/>
                                    </div>

                                    <input type="hidden" name="redirect" t-att-value="redirect"/>
                                </form>
                            </div>
                        </div>
                    </div>
                        <div class="sb-footer-extra">
                            <div class="sb-footer-extra-title">SkillBridge Studio</div>
                            <div class="sb-footer-extra-note">Crafted for modern learning experiences.</div>
                            <a class="sb-footer-extra-link" href="mailto:support@eduskillbridge.net">support@eduskillbridge.net</a>
                        </div>
                </div>
            </t>
        </xpath>
    </template>

    <!-- Re-skin OAuth providers -->
    <template id="skillbridge_auth_providers" inherit_id="auth_oauth.providers" priority="1000">
        <xpath expr="//t[@t-if]" position="replace">
            <t t-if="len(providers) &gt; 0">
                <div class="sb-social-block">
                    <div class="sb-social-divider"><span>Or continue with</span></div>
                    <div class="sb-social-buttons">
                        <a t-foreach="providers" t-as="p" class="sb-social-btn" t-attf-href="#{p['auth_link']}">
                            <i t-att-class="p['css_class']"/>
                            <span t-esc="p['body']"/>
                        </a>
                    </div>
                </div>
            </t>
        </xpath>
    </template>

</odoo>
